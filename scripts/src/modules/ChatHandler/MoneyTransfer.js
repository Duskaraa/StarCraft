import { system, world } from "@minecraft/server";
import { ModalFormData } from "@minecraft/server-ui";

export function MoneyTransferMenu() {
  const mForm = new ModalFormData() 
  mForm.title("§fTransferencia de dinero")
}